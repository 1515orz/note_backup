# 文件查看命令

## ls命令
`-a` 显示隐藏文件
`-l` 以列表形式显示所有文件的详细信息
`-h` 结合-l使用，人性化显示文件大小（kb）
`-d` 可以显示文件夹本身,而不显示其中的内容, 选项可以结合起来用 例如-alh

## lsattr命令
`lsattr [选项][文件名]`查看文件系统属性
`-a` 显示所有文件和目录
`-d` 若目标是目录, 仅列出目录本身的属性, 而不是子文件的







 
