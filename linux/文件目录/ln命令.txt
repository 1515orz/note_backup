ln命令是link，作用是生成链接文件，相当于“快捷方式”
用法： ln 文件目录 链接生成目录和名字

-s 创建软链接，软连接的文件都是lrwx，文件也很小
ln 是直接生成硬链接，硬链接并不常用
硬链接不能跨分区，不能针对目录使用
但是软连接可以跨分区，软连接可以对目录使用

软连接是一个指向，仅仅是个快捷方式
硬链接可以保持和源文件同时更新，源文件就算丢失，硬链接仍然可以访问

可以通过 ls -i 来查看链接的文件
