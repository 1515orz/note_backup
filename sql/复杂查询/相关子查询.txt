关联子查询
SELECT *
FROM employees e
WHERE salary > (
	SELECT AVG(salary)
    FROM employees
    WHERE office_id = e.office_id
)

WHERE 相当于一次遍历
e.office_id 相当于一个判断条件,使用别名作为外查询名,和子查询关联
效果就是对每一个项目进行遍历判断，条件是office_id相等

原理:在子查询中引用了外查询,会产生关联,并循环执行子查询条件