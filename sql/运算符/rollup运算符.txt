WITH ROLLUP
将聚合值得列相加，在底部得到结果

SELECT 
    client_id,
    SUM(invoice_total) AS total_sales
FROM invoices
GROUP BY client_id WITH ROLLUP

当你使用多列分组，并运用rollup时，会得到每个组相加结果，并且最后再汇总

* 这个用法只有在 mysql中可以使用， sql server 和 oracle中不行