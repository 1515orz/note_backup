# 插入多行

    插入多行的操作类似插入单行
    只需要在VALUES()后的值加','

> 一次插入单列的多行数据
```
INSET INTO table (column)
VALUES (v1),
      (v2),
      (v3),
      ....
```

> 一次插入多行包含多列的数据
```
INSERT INTO table (c1, c2, c3)
VALUES (a1, a2, a3),
      (b1, b2, b3),
      (v1, v2, v3),
      ....
```