# 视图优点

    减少更改数据库带来的工作量, 可以在视图中使用别名来重命名, 可以使用视图限制基础表的访问, 可以在你的数据上放置一层视图, 来减少改动数据库带来的巨大影响(简单, 复用, 安全)


1.可以简化查询
2.可以进行权限控制
3.大数据分表时可以用到
比如：表的行数超过200万行时, 就会变慢
可以把一张表的数据拆成4张表来存放

还可以使用视图, 把4张表形成一张视图

视图是表的查询结果, 如果表的数据改变了, 那么自然会影响视图的结果,  反之亦然