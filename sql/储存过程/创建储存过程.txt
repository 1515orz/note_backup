```
DELIMITER $$
CREATE PROCEDURE get_clients()
BEGIN 
	SELECT * FROM clients;
END $$

DELIMITER ;
```



储存过程首先要声明分隔符，使用DELIMITER ，最后可以再改回分号

使用mysql创建储存过程：
右键 stored procedure 创建新的储存过程,再点击apply，
mysql会自动生成修改分隔符语句